import React from 'react';

import Layout from '@/components/Layout';

export default () => (
    <Layout>
        <div className="container">
            <h1>Hello, World!</h1>
        </div>
    </Layout>
);
